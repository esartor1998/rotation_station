{
  "name": "obj-model-preview",
  "version": "1.0.0",
  "description": "Interactive Three.js preview for .obj models — upload a file or load one from a URL.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
